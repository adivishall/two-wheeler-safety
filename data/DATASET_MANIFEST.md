# Dataset manifest — `master_traffic_violation_dataset`

Generated by `dataset_manifest.py`; every count is read from the label files on disk. Fields that cannot be derived from the files are recorded as `UNVERIFIED` rather than guessed.

- **version**: `sha256:bef022b6e9fd485e`
- generated: 2026-09-13T13:07:38.608136+00:00
- config: `master_traffic_violation_dataset/data.yaml`
- source: **UNVERIFIED**
- licence: **UNVERIFIED**

## Classes

| id | class |
|---:|---|
| 0 | `Plate` |
| 1 | `WithHelmet` |
| 2 | `WithoutHelmet` |
| 3 | `TripleRiding` |

## Annotation format

YOLO detection: one .txt per image under a parallel labels/ tree, each line 'class cx cy w h' normalised to [0,1]. A missing or empty .txt is a background image.

## Splits

| split | images | label files | background | instances |
|---|---:|---:|---:|---:|
| `train` | 11195 | 11195 | 453 | 21191 |
| `val` | 383 | 383 | 76 | 572 |
| `test` | 194 | 194 | 31 | 311 |
| **total** | **11772** | **11772** | **560** | **22074** |

### Per-class instances

| class | train | val | test | total |
|---|---:|---:|---:|---:|
| `Plate` | 6605 | 247 | 131 | **6983** |
| `WithHelmet` | 4862 | 27 | 27 | **4916** |
| `WithoutHelmet` | 4862 | 209 | 105 | **5176** |
| `TripleRiding` | 4862 | 89 | 48 | **4999** |

## Preprocessing

Training images are pre-augmented offline: every train/images file is named aug_<hash>.jpg with a paired .npy cache, so augmentation is baked into the stored dataset rather than applied on the fly. The exact transforms are UNVERIFIED.

## Split hygiene

Audited with `audit_dataset.py` — **leakage found**.

Method: dHash(8x8, 64-bit) with Hamming distance <= 5; catches recompression/resize/photometric duplicates, not heavy geometric augmentation

| split | held-out images | leaked | rate |
|---|---:|---:|---:|
| `test` | 194 | 19 | 9.8% |
| `val` | 383 | 31 | 8.1% |

## Known limitations

- Source datasets, their authors and their licences are not recorded with the data; do not redistribute the images or assert a licence until traced.
- Validation and test splits are small and imbalanced (WithHelmet has only 27 instances in each), so per-class metrics for WithHelmet are high-variance and should be read as indicative.
- Imagery skews to Indian road scenes and the plate decoder assumes the Indian plate format.
- The test split comes from the same pool as train/val, so it shares their biases; it measures held-out performance, not generalisation to a different camera or city.
- A Plate box being detected says nothing about whether OCR can read it; OCR quality is evaluated separately.

## Reproducing this file

```bash
python3 dataset_manifest.py --data master_traffic_violation_dataset/data.yaml \
    --leakage eval/results/dataset_leakage.json
```

