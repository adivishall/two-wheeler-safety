# Résumé evidence

Every number on this page was measured by tooling in this repository, on data
present on the machine, and can be regenerated by a named command. Nothing is
estimated or rounded in the flattering direction.

The rule for using this page: **if a bullet contains a number, the command that
produced it is one line below.** If you cannot run the command, do not use the
number.

---

## Three bullets (use these)

> **Built and evaluated an end-to-end two-wheeler violation vision system** —
> YOLOv8 detection → multi-object tracking → Hungarian plate↔rider association →
> temporal OCR stabilization → violation state machines → confidence scoring →
> evidence packaging → review dashboard — measured at every layer, not just the
> detector: **mAP@50 0.727 / mAP@50-95 0.532** on a de-leaked held-out test
> split, and **precision 1.00 / recall 1.00** on the end-to-end fine decision
> across multi-vehicle scenarios.

> **Fixed and proved the tracking/association layer.** Root-caused an ID-switch
> bug in which two vehicles crossing under heavy overlap were merged into one
> track, and fixed it at the association merge thresholds — **13 → 0 ID switches**
> and association accuracy **0.760 → 0.989** across eight crossing/overlap
> scenarios. Then benchmarked the full pipeline against a naive single-frame
> detector on identical inputs (**1.50 → 0.00 mean false positives**, even with a
> perfect detector) and single-frame vs temporal OCR (wrong-plate rate
> **85% → 30% → 2%** across last-frame, best-confidence and the shipped stabilizer
> at 12% character noise) — temporal voting converts OCR errors into abstentions,
> the correct trade for a system that fines people.

> **Built an ML evaluation and performance discipline, not just a model.**
> Perceptual-hash split audit (**9.8% of the test set** near-duplicates of
> training images, re-measured de-leaked); A/B checkpoint comparison that caught a
> "helmet fix" model silently forgetting a whole class (**TripleRiding mAP@50
> 0.000**); a fault-injection error budget identifying **OCR as 76.8% of system
> sensitivity**, redirecting effort away from the detector; and a measured
> **+74.9% throughput gain** (28.7 → 50.2 FPS) from eliminating redundant OCR
> calls with a byte-identical recorded fine — all backed by 466 model-free tests
> at 94.4% branch coverage, ruff/mypy clean.

---

## The measurements behind them

### Detector (`traffic-4class@1.0.0`, de-leaked held-out test, 175 images)

| metric | value |
|---|---:|
| mAP@50 | 0.7265 |
| mAP@50-95 | 0.5320 |
| mean precision / recall | 0.756 / 0.785 |
| `TripleRiding` mAP@50 | 0.950 |
| `Plate` mAP@50 | 0.863 |
| `WithoutHelmet` mAP@50 | 0.705 |
| `WithHelmet` mAP@50 | 0.387 *(27 instances — high variance)* |
| inference latency | 27.9 ms/image on Apple M4 (MPS) |

```bash
python3 evaluate_model.py --model runs/detect/traffic_model-2/weights/best.pt \
    --data eval/clean_splits/data.yaml --split test --save-artifacts eval --benchmark
```

### Split hygiene

| split | images | leaked | rate |
|---|---:|---:|---:|
| `val` | 383 | 31 | 8.1% |
| `test` | 194 | 19 | 9.8% |

De-leaking moved mAP@50 **+0.006** — the leakage was *not* inflating the result.
Say that, not "leakage was inflating our metrics"; the honest version is more
interesting and is what the data shows.

```bash
python3 audit_dataset.py --data master_traffic_violation_dataset/data.yaml \
    --write-clean-split eval/clean_splits
```

### Model A/B (same split, same settings)

| model | mAP@50 | `WithHelmet` | `TripleRiding` |
|---|---:|---:|---:|
| `traffic_model_probe` | 0.741 | 0.470 | 0.882 |
| `traffic_model-2` (ships) | 0.727 | 0.387 | 0.950 |
| `traffic_model_helmetfix` | 0.500 | 0.417 | **0.000** |

```bash
python3 compare_models.py --data eval/clean_splits/data.yaml --split test
```

### Pipeline (synthetic scenarios through the real runtime code)

| metric | value |
|---|---:|
| end-to-end fine precision / recall | 1.00 / 1.00 (TP 8, FP 0, FN 0) |
| `no_helmet` decision F1 | 1.00 |
| `triple_riding` decision F1 | 1.00 |
| helmet precision at `confirm_window=1` | **0.67** |
| helmet precision at `confirm_window≥2` | **1.00** (no recall cost) |
| ID switches across 8 crossing/overlap scenarios | **0** *(was 13 pre-fix)* |
| association accuracy on those scenarios | 0.760 → **0.989** |

### Pipeline vs a naive single-frame detector (identical inputs)

| detector noise | naive F1 (mean FPs) | pipeline F1 (mean FPs) |
|---|---:|---:|
| 0.00 | 0.912 (1.50) | **1.000 (0.00)** |
| 0.10 | 0.840 (3.42) | **0.994 (0.03)** |
| 0.20 | 0.820 (4.08) | **0.962 (0.23)** |
| 0.40 | 0.808 (4.47) | **0.849 (0.72)** |

Naive recall is 1.000 throughout — it fines on anything — so the whole
difference is precision. The naive policy is also handed perfect plate↔vehicle
association for free, so this is a lower bound on the pipeline's advantage.

### OCR decision policy (simulated character noise, 12% substitution)

| policy | coverage | accuracy when it answers |
|---|---:|---:|
| last frame | 1.00 | 0.150 |
| best confidence | 1.00 | 0.700 |
| **temporal stabilizer** | 0.51 | **0.980** |

### Error budget (equal 30% degradation per stage)

| stage | end-to-end F1 drop | share |
|---|---:|---:|
| OCR | −0.453 | **76.8%** |
| detector class confusion | −0.090 | 15.3% |
| detector recall | −0.043 | 7.3% |
| plate dropout | −0.003 | 0.6% |

A *corrupted* plate is catastrophic (it fines a real but wrong vehicle); a
*missing* plate is nearly free, because other frames recover it.

### Speed estimation

Best MAE **11.3 km/h** (homography) for motion toward the camera; a constant
pixels-per-metre calibration has **0.00 overspeed recall** in that geometry.

```bash
python3 evaluate_pipeline.py      # all pipeline numbers above
```

### Application

| metric | value |
|---|---:|
| video throughput (OCR lock on) | 50.2 FPS (5 OCR calls) |
| video throughput (OCR every plated frame) | 28.7 FPS (120 OCR calls) |
| OCR-lock speedup | **+74.9%**, identical recorded fine |
| single-image inference | 24.2 ms (≈41 FPS) |
| EasyOCR on a plate crop | 14.2 ms |
| model load (one-time) | ≈2.9 s |
| peak RSS | 987 MB |
| per-frame time split | 88% YOLO, 9% OCR, <2% everything else |

```bash
python3 benchmark.py --model runs/detect/traffic_model-2/weights/best.pt \
    --image test.jpg --video demo_traffic.mp4 --max-frames 120 --ocr-lock-ab
```

The OCR lock is a **measured** speedup with an identical recorded fine, not an
assumed optimization. Worth mentioning in an interview: the previously
documented "+172%" did not reproduce, because the benchmark never forwarded the
lock setting and both arms ran with it on. Finding and fixing that is the
better story.

### Engineering

| metric | value |
|---|---:|
| tests | 466, model-free, ~5 s |
| lint / types | `ruff` clean, `mypy` clean |
| CI | GitHub Actions, Python 3.11 + 3.12, no GPU/weights/dataset needed |

---

## What to say if asked "how do you know?"

Every claim above has a command. The generated JSON/Markdown/CSV behind them is
committed in `eval/results/`, and the docs cite those files rather than
hand-transcribed numbers — so a reviewer can diff the docs against the data.

---

## What NOT to claim

Being able to state these clearly is worth more in an interview than an extra
decimal place:

- **Not "accurate in the real world".** The test split comes from the same pool
  as training and shares its biases. It measures held-out performance, not
  generalisation to a new camera or city.
- **Not "calibrated confidence".** It is a model score. It ranks correctness well
  for `WithoutHelmet` (Spearman 1.00) and not at all for `TripleRiding` (−0.20).
  "0.8" does not mean "80% correct".
- **Not "OCR accuracy of X%".** No labelled plate-sequence set exists here, so
  the OCR comparison uses *simulated* character noise. It measures the decision
  rule, not EasyOCR.
- **Not "counts riders".** One `TripleRiding` class; three-up and four-up are the
  same detection.
- **Not "radar-grade speed".** Best measured MAE 11.3 km/h, requires a surveyed
  homography.
- **Not "pipeline precision 1.00 means the system is perfect".** That is a
  ceiling on what the synthetic suite tests — no motion blur, no crowds, no
  detector noise — and the suite was written by the same person as the pipeline.
- **Not "production traffic enforcement".** It is a detection and review
  assistant; a human confirms every flag, and it never resolves owner identity.

---

## Technologies actually used

Python, Ultralytics YOLOv8, EasyOCR, OpenCV, NumPy, Flask, SQLite, gunicorn,
Docker, pytest, ruff, mypy, GitHub Actions.

Concepts: multi-object tracking, Hungarian assignment, temporal state machines,
OCR post-processing and voting, perceptual hashing for dataset hygiene, camera
calibration and homography for speed, confidence analysis, fault-injection error
budgeting, model versioning and provenance manifests, REST API design,
background job management.

See also [RESUME_NOTES.md](RESUME_NOTES.md) for the longer engineering narrative.
